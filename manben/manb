#!/bin/bash

edit_flag=false

while getopts 'e' flag; do
  case "$flag" in
    e) edit_flag='true' ;;
  esac
done

if [ $edit_flag = true ]
then 
   vim "$HOME/.custom-config/manben/$2.ben"
else
   less "$HOME/.custom-config/manben/$1.ben"
fi
